# NOTE: pyproject.toml is the source of truth for dependencies (used by
# `pip install -e .`). This file is kept only for tools that expect a
# requirements.txt and may drift out of sync — prefer pyproject.toml.
cognee[fastembed]>=0.1.0
typer[all]>=0.9.0
fastapi>=0.100.0
uvicorn>=0.22.0
gitpython>=3.1.30
fastmcp>=0.1.0
python-dotenv>=1.0.0
pydantic>=2.0.0
jinja2>=3.1.2
groq>=0.9.0
pathspec>=0.12.1
